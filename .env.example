# JWT Configuration
JWT_SECRET=your-jwt-secret-key-here-minimum-32-characters

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://your-frontend-domain.vercel.app

# OAuth2 Configuration - Google
# For Google OAuth App configuration (Google Cloud Console):
# - Authorized JavaScript origins: http://localhost:8080 (dev) or https://your-domain.com (prod)
# - Authorized redirect URIs: http://localhost:8080/login/oauth2/code/google (dev)
#   or https://your-domain.com/login/oauth2/code/google (prod)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:8080/login/oauth2/code/google

# OAuth2 Configuration - GitHub
# For GitHub OAuth App configuration:
# - Homepage URL: http://localhost:8080 (for development) or https://your-domain.com (for production)
# - Authorization callback URL: http://localhost:8080/login/oauth2/code/github (for development)
#   or https://your-domain.com/login/oauth2/code/github (for production)
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret
GITHUB_REDIRECT_URI=http://localhost:8080/login/oauth2/code/github

# Stripe Configuration
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret
STRIPE_SUCCESS_URL=http://localhost:3000/payment/success
STRIPE_CANCEL_URL=http://localhost:3000/payment/cancel

# Paddle Configuration
PADDLE_API_URL=https://sandbox-api.paddle.com
PADDLE_VENDOR_ID=your-paddle-vendor-id
PADDLE_API_KEY=your-paddle-api-key
PADDLE_PUBLIC_KEY=your-paddle-public-key
PADDLE_WEBHOOK_SECRET=your-paddle-webhook-secret
PADDLE_ENVIRONMENT=sandbox
PADDLE_PRODUCT_ID=your-paddle-product-id
PADDLE_WEBHOOK_URL=your-paddle-webhook-url

# MySQL Database Configuration
# For local development, use localhost MySQL
# For Railway deployment, use Railway's MySQL service URL
DATABASE_URL=jdbc:mysql://localhost:3306/teamwork_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
DATABASE_USERNAME=your-mysql-username
DATABASE_PASSWORD=your-mysql-password

# Security Configuration
COOKIE_SECURE=false

# Redis Configuration (Optional)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=